# Provider selection must be explicit so the runtime does not fall back to HCX.
CLAFACT_CLAIM_PROVIDER=openai

# Put real values only in a local .env or the FlyHermes secret store.
OPENAI_API_KEY=
KOSIS_API_KEY=
